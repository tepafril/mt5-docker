FROM gmag11/metatrader5_vnc

USER root

# Install Python3 and psycopg2 for PostgreSQL connectivity
RUN apt-get update && apt-get install -y --no-install-recommends \
    python3 \
    python3-psycopg2 \
    && apt-get clean && rm -rf /var/lib/apt/lists/*
